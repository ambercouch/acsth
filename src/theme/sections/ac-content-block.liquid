{%- comment -%}
  This is a required section for the Shopify Theme Store.
  It is available when you add the "Image with text" section in the theme editor.

  Theme Store required settings
  - Image
  - Heading: title of the block
  - Text: description of the block

  Theme Store optional settings
  - Button label: label associated with the button
  - Button link: link of the button
{%- endcomment -%}

{%- assign block_layout =  section.settings.cb_layout -%}


<section id="{{ section.settings.title | handle }}" class="ac-json-schema l-main__content-block--{{ section.id }} u-bgc-{{ cb_background_colour_class  }} {{ cb_layout_desk_width_50_class }} is-image-layout-{{ cb_image_layout }} {{ cb_desktop_image_class }}" data-section-id="{{ section.id }}" data-section-type="content-block" >
  <div class="c-content-block">
    <div class="c-content-block__row">
      {% if cb_layout == '5050' %}
      <div class="c-content-block__col">
        <div class="c-content-block__content">
          <div class="c-content-block__content">
            <h2 class="c-content-block__heading"></h2>
            <div class="c-content-block__body"></div>
            <div class="c-content-block__footer"></div>
          </div>
        </div>
      </div>
      <div class="c-content-block__col">
        <div class="c-content-block__content">
          <h2 class="c-content-block__heading"></h2>
          <div class="c-content-block__body"></div>
          <div class="c-content-block__footer"></div>
        </div>
      </div>
      {% endif %}
    </div>
  </div>
</section>

{% schema %}
{
  "name": "Content Block",
  "settings": [
    {
      "type": "text",
      "id": "title",
      "label": "Heading",
      "default": "Block Heading"
    },
    {
      "type": "checkbox",
      "id": "hide_title",
      "label": "Hide the block heading",
      "default": true
    },
    {
      "type": "select",
      "id": "block_layout",
      "label": "Block Layout",
      "default": "5050",
      "options": [
        {
          "value": "5050",
          "label": "50/50"
        }
      ]
    },
    {
      "type": "select",
      "id": "block_background_colour",
      "label": "Block Background Colour",
      "default": "1",
      "options": [
        {
          "value": "1",
          "label": "Brand Colour 1"
        }
      ]
    }
  ,
    {
      "type": "image_picker",
      "id": "block_background_image",
      "label": "Block Background Image"
      }
  ],
  "blocks": [
    {
      "name": "Content Section",
      "type": "content",
      "settings": [
        {
          "type": "text",
          "id": "content-title",
          "label": "Content Title",
          "default": "Title"
        },
        {
          "type": "richtext",
          "id": "content-body",
          "label": "Content Body",
          "default": "<p>Add some basic text</p>"
        },
        {
          "type": "html",
          "id": "content-body-html",
          "label": "Content HTML",
          "default": "<p>Add some html code</p>"
        },
        {
          "type": "text",
          "id": "content-btn-1-label",
          "label": "Button 1 Label",
          "default": "Shop Now"
        },
        {
          "type": "url",
          "id": "content-btn-1-link",
          "label": "Button 1 link"
        },
        {
          "type": "text",
          "id": "content-btn-2-label",
          "label": "Button 2 Label",
          "default": "Find Out More"
        },
        {
          "type": "url",
          "id": "content-btn-2-link",
          "label": "Button 2 link"
        }
      ]
    }
  ],
  "presets": [{
    "name": "Content Block",
    "category": "Content"
  }]
}
{% endschema %}
